import { css } from 'lit';

export default css`
  pre {
    padding: 1rem;
    overflow: auto;
    background: var(--codeblock-bg, #0b1021);
    color: var(--codeblock-fg, #e5e7eb);
    border-radius: 0.375rem;
  }
`;


